# nivi
#include<stdio.h>
#include<conio.h>
void main()
{
int n;
printf("enter the no:");
scanf("%d",&n);
if(n>0)
{
printf("the no is pos",n);
elseif(n<0)
{
printf("the no is neg",n);
else
printf("the no is zero",n);
}
}
}
